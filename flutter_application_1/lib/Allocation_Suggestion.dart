import 'package:flutter/material.dart';
class Allocation_Suggestion extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: const Text('Allocation Suggestion')
      ),
    );
  }

}